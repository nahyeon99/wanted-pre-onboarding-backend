# common
spring:
  config:
#    name: classpath:application.yml, /Users/nahyeon/dev/externalfiles/application-local.yml
    name: classpath:application.yml, /home/ec2-user/app/application-prod.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      use-new-id-generator-mappings: false

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher